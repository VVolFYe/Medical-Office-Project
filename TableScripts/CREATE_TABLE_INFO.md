# Cum se ruleaza scripturile:

- Scripts -> CabinetSchema -> run: asta sterge tabelele ce existatu de dinainte si apoi le creeaza.

- Scripts -> CabinetData -> run: script-ul asta populeaza tabelele.

- Am adaugat aici in Folder si un CheckTables.sql pe care il putem folosi la SQL Commands ca sa verificam daca au fost create tabelele cu succes. Nu e necesar, e pentru debugging mai mult.